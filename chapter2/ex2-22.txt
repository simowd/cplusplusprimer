Exercise 2.22: Assuming p is a pointer to int, explain the following code:
if (p) // Verifies that the pointer points to a pointer and it's not null. 
if (*p) // Verifies that the value of the pointer is not zero. 
